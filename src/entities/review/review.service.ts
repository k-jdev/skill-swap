import { createClient } from "@/shared/utils/supabase/client";
import { Review } from "./model";

// export async function addReview(userId: string) {
//   const supabase = createClient();

//   const { data, error } = supabase.from("reviews").insert();
// }
